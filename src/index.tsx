import React from 'react';
import ReactDOM from 'react-dom';
import App from './App.tsx';  // Certifique-se de que o caminho está correto e o nome do arquivo está correto

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);